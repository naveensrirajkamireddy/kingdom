schema: "http://localhost:4000/kingdom-gateway" # or the path to your local schema file
documents: "src/gql/**/*.gql" # Or wherever your components are stored with queries/mutations
generates:
  src/graphql/generated.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
